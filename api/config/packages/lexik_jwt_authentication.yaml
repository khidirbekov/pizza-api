lexik_jwt_authentication :
    secret_key: '%kernel.project_dir%/config/jwt/private.pem'  # требуется для создания токена 
    public_key: '%kernel.project_dir%/config/jwt/public.pem'   # требуется для проверки токена 
    pass_phrase: 'pizza'  # требуется для создания токена, рекомендуется использовать переменную окружения 
    token_ttl: 3600000
